import {StyleSheet} from 'react-native';
import {Colors} from '../../../../theme';

export const styles = StyleSheet.create({
    flatList: {
        paddingBottom: 120,
    },
    textEmpty: {
        color: Colors.text,
        marginTop: 20,
        textAlign: 'center',
    },
});
